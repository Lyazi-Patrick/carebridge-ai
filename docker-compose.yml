services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: carebridge
      POSTGRES_USER: carebridge
      POSTGRES_PASSWORD: carebridge_local_only
    ports:
      - "5432:5432"
    volumes:
      - carebridge_postgres_data:/var/lib/postgresql/data

volumes:
  carebridge_postgres_data:
